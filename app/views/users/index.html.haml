%h1 社員一覧

%table.table.table-striped.table-hover
  %thead
    %tr
      %th 所属
      %th 氏名
      %th 派遣先
      %th 先月勤務表の状態
      %th メモ

  %tbody
    - @users.each do |user|
      %tr
        %td= user.company.name
        %td
          = link_to '変更', edit_user_path(user), method: :get, class: 'btn btn-primary' if current_user.try(:admin?)
          = user.name
        %td= user.partner.try(:name)
        - btn_class = (get_roster_status(user.rosters.try(:reverse).try(:second).try(:status)) == '未編集') ? 'btn-warning' : 'btn-success'
        %td= link_to get_roster_status(user.rosters.try(:reverse).try(:second).try(:status)), company_user_rosters_path(user.company, user), method: :get, class: "btn #{btn_class}"
        %td= user.rosters.try(:reverse).try(:second).try(:memo)

